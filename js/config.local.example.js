// Copiar este archivo como js/config.local.js y completar con las credenciales reales.
// js/config.local.js está en .gitignore — nunca se sube al repo.

export const INCITI_API_KEY = '';
export const INCITI_API_URL = '';

// Proxy local para desarrollo (resuelve CORS). Correr: python scripts/proxy.py
// Dejar vacío ('') para llamar directo a Inciti (requiere CORS habilitado).
export const INCITI_PROXY_URL = 'http://localhost:5050';
